# AI-Powered Development Assistant Configuration

# === Required Configuration ===

# Gemini AI API Key
# Get yours at: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# GitHub Personal Access Token
# Create one at: https://github.com/settings/tokens
# Required scopes: repo, workflow
GITHUB_TOKEN=your-github-token-here

# Database Connection
# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@localhost:5432/builder-6

# === Optional: Alternative LLM Providers ===

# Select which LLM provider to use
# Options: gemini (default), openai, anthropic
# LLM_PROVIDER=gemini

# OpenAI API Key (required if LLM_PROVIDER=openai)
# Get yours at: https://platform.openai.com/api-keys
# OPENAI_API_KEY=your-openai-api-key

# Anthropic API Key (required if LLM_PROVIDER=anthropic)
# Get yours at: https://console.anthropic.com/
# ANTHROPIC_API_KEY=your-claude-api-key

# === Optional: Docker Configuration ===

# Docker socket path (auto-detected if not set)
# Default: Checks for OrbStack first, then /var/run/docker.sock
# DOCKER_SOCKET_PATH=/var/run/docker.sock

# Container name prefix
# Default: builder6-container-
# DOCKER_CONTAINER_PREFIX=myapp-container-

# Maximum number of containers per group
# Default: 5
# DOCKER_CONTAINER_LIMIT=10

# Idle container cleanup timeout (milliseconds)
# Default: 600000 (10 minutes)
# DOCKER_IDLE_TIMEOUT=1800000

# Default Docker image for containers
# Default: debian:stable-slim
# DOCKER_DEFAULT_IMAGE=ubuntu:latest

# === Optional: Testing Configuration ===

# GitHub username for integration tests
# GITHUB_TEST_OWNER=your-github-username

# Repository name for integration tests
# GITHUB_TEST_REPO=test-repo-name

# === Optional: Retry Configuration ===

# Maximum number of retries for 503 errors
# Default: 10
# LLM_MAX_RETRIES=10

# Initial retry delay in milliseconds
# Default: 1000 (1 second)
# LLM_INITIAL_RETRY_DELAY=1000

# Maximum retry delay in milliseconds
# Default: 10000 (10 seconds)
# LLM_MAX_RETRY_DELAY=10000

# Exponential backoff factor
# Default: 2
# LLM_RETRY_BACKOFF_FACTOR=2

# === Optional: Debug Options ===

# Enable debug logging
# Default: false
# DEBUG=true

# Disable Prisma warnings
# Default: false
# PRISMA_DISABLE_WARNINGS=true
